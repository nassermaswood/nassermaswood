P = int(input('Enter: Principal Amount '))
N = int(input('Enter: Period (Months) '))
R = float(input('Enter: Rate of Interest '))
simple_interest = (P*N*R)/100
print('The simple interest is:', simple_interest)
